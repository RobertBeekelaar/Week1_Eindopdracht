    // else {

    //   appointments.forEach(element => {
    //     if (element.dentist === dentist)
    //     {
    //     alert("The dentist is already scheduled this timeslot")
    //      return
    // }
    

    // else {
    //   appointments.forEach(element => {
    //     if (element.dentist === dentist)
    //     {
    //       alert("The dentist is already scheduled this timeslot")
    //       return
    //   }
    //   appointments.dentist === dentist 
    //   appointments.time === time &&
    //   appointments.day === setAppointments.day &&
    //   appointments.week === setAppointments.week
    // ) {
    //   alert("The dentist is already scheduled this timeslot")
    //   return
    // }